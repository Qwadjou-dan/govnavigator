{
  "name": "govnavigator-web",
  "version": "0.3.1",
  "private": true,
  "description": "GovNavigator Ghana - mobile-first PWA for finding government services",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000"
  },
  "//": "Caret ranges, not exact pins: an exact pin freezes you on a version that later turns out to have a CVE, which is what happened with next 15.1.6 (CVE-2025-66478). ^15.5.23 keeps you on patched 15.x. Do not jump to Next 16 without testing - it is a major release.",
  "dependencies": {
    "next": "^15.5.23",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.23",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "postcss": "^8.5.23"
  },
  "//overrides": "Forces the patched postcss into Next's nested copy too. sharp is deliberately NOT overridden: the fix is only at 0.35.0-rc, and we never call next/image, so the flagged libvips code path is never executed. Revisit when sharp 0.35.0 goes stable."
}
