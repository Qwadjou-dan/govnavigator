{
  "name": "govnavigator",
  "version": "0.3.1",
  "private": true,
  "description": "GovNavigator Ghana - run the FastAPI backend and Next.js frontend from one terminal.",
  "//": "Backend scripts go through scripts/py.mjs, which resolves the virtualenv's interpreter for the current OS. They used to hardcode .venv\\Scripts\\python.exe, so every one of them failed on macOS and Linux.",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node scripts/py.mjs -m uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "node scripts/setup-backend.mjs",
    "setup:frontend": "cd frontend && npm install && node -e \"const fs=require('fs');fs.existsSync('.env.local')||fs.copyFileSync('.env.local.example','.env.local')\"",
    "seed": "node scripts/py.mjs -m app.scripts.seed",
    "seed:reset": "node scripts/py.mjs -m app.scripts.seed --reset",
    "check:db": "node scripts/py.mjs -m app.scripts.check_db",
    "evaluate": "node scripts/py.mjs -m app.scripts.evaluate",
    "test": "node scripts/py.mjs -m pytest"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
